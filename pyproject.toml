[project]
name = "agent-collab"
version = "0.1.0"
description = "Project that allows multiple agents to work together to complete a given task"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiocache>=0.12.3",
    "browser-use>=0.7.9",
    "fastapi>=0.125.0",
    "google-genai>=1.38.0",
    "pydantic>=2.11.9",
]
